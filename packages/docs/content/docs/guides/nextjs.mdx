---
title: Next.js
description: Use Polyterm with Next.js
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Callout } from 'fumadocs-ui/components/callout'

<Steps>
<Step>

## Install

<Tabs items={['bun', 'npm', 'yarn', 'pnpm']}>
<Tab value="bun">
```bash title="Terminal"
bun add @polyterm.io/web
```
</Tab>
<Tab value="npm">
```bash title="Terminal"
npm install @polyterm.io/web
```
</Tab>
<Tab value="yarn">
```bash title="Terminal"
yarn add @polyterm.io/web
```
</Tab>
<Tab value="pnpm">
```bash title="Terminal"
pnpm add @polyterm.io/web
```
</Tab>
</Tabs>

</Step>
<Step>

## Configure Next.js

Use the `withPolyterm` plugin to configure webpack automatically:

```ts title="next.config.ts"
import { withPolyterm } from "@polyterm.io/web/next-plugin"

export default withPolyterm({
  // ... other Next.js config
})
```

If you provide your own `webpack` function, `withPolyterm` will chain it —
your config runs first, then opentui aliases and shims are applied on top.

</Step>
<Step>

## Create Your Component

Mark your page as a client component and use `TUI`:

```tsx title="app/page.tsx"
"use client"

import { TUI } from "@polyterm.io/web"

function PolytermDemo() {
  return (
    <box border borderStyle="rounded" padding={1}>
      <text fg="#a3be8c">Hello from Next.js!</text>
    </box>
  )
}

export default function Home() {
  return (
    <TUI style={{ width: "100vw", height: "100vh" }}>
      <PolytermDemo />
    </TUI>
  )
}
```

</Step>
</Steps>

## SSR Safety

<Callout type="success">
`TUI` is SSR-safe by default. On the server it renders an empty
container div. On the client it hydrates and creates the canvas. No
`dynamic(() => import(...), { ssr: false })` wrapper is needed.
</Callout>
